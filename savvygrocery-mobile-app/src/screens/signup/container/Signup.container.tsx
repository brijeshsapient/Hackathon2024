import React from 'react';
import SignupView from '../view/Signup.view';

const SignupContainer = props => {
  return <SignupView navigation={props.navigation} />;
};

export default SignupContainer;
