import React from 'react';
import SettingsView from '../view/Settings.view';

const SettingsContainer = props => {
  return <SettingsView navigation={props.navigation} />;
};

export default SettingsContainer;
